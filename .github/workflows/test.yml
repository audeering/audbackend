name: Test

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main, dev ]

# The MinIO community server is archived:
# https://github.com/minio/minio is read-only,
# and dl.min.io answers "410 Gone" for every community release and hotfix.
# RELEASE.2025-09-07T16-13-09Z is the last version we can run,
# and the Docker Hub `minio/minio` repository it used to come from is deleted.
# The two surviving copies of that release are the quay.io image
# and the binaries attached to the GitHub release,
# which is where the jobs below now get it from.
env:
  MINIO_RELEASE: RELEASE.2025-09-07T16-13-09Z

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [ '3.10', '3.11', '3.12', '3.13' ]
        include:
          - python-version: '3.14'
          - python-version: '3.14'
            minio: '7.2.18'

    # Self-hosted MinIO instead of the public play.min.io playground.
    env:
      AUDBACKEND_TEST_MINIO_HOST: localhost:9000
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
      MINIO_CONFIG_FILE: ${{ github.workspace }}/.github/minio-test.cfg

    steps:
    - uses: actions/checkout@v7

    - name: Start MinIO
      run: |
        docker run -d --name minio -p 9000:9000 \
          -e MINIO_ROOT_USER=minioadmin \
          -e MINIO_ROOT_PASSWORD=minioadmin \
          quay.io/minio/minio:"$MINIO_RELEASE" server /data --console-address ":9001"

    - name: Wait for MinIO
      run: |
        for i in $(seq 1 60); do
          if curl -sf http://localhost:9000/minio/health/live; then
            echo "MinIO is up"
            exit 0
          fi
          sleep 1
        done
        echo "MinIO did not become healthy in time"
        docker logs minio
        exit 1

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v7
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install uv
      uses: astral-sh/setup-uv@v9.0.0
      with:
        cache-suffix: ${{ github.workflow }}

    - name: Sync Python environment
      run: uv sync

    - name: Install older minio version
      run: uv pip install minio==${{ matrix.minio }}
      if: matrix.minio == '7.2.18'

    - name: Test file-system only package
      run: |
        uv run --no-default-groups --group test-filesystem --isolated pytest --cov-fail-under=0 tests/test_minimal_install.py tests/test_backend_filesystem.py

    - name: Test with pytest
      run: |
        uv run pytest --cov-config=.coveragerc.${{ runner.os }}

    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v7
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        files: ./coverage.xml
      if: matrix.python-version != '3.14'

  build-other-os:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ windows-latest, macOS-latest ]
        python-version: [ '3.12' ]

    # Docker is not pre-installed on GitHub's Windows/macOS runners,
    # so we run the native MinIO server binary.
    # 127.0.0.1 is used instead of localhost throughout: on Windows,
    # localhost can resolve to IPv6 ::1 while the server listens on
    # IPv4 only, making every connection attempt fail.
    env:
      AUDBACKEND_TEST_MINIO_HOST: 127.0.0.1:9000
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
      MINIO_CONFIG_FILE: ${{ github.workspace }}/.github/minio-test.cfg

    steps:
    - uses: actions/checkout@v7

    - name: Start MinIO (macOS)
      if: runner.os == 'macOS'
      run: |
        # -f so a dead URL fails here loudly,
        # instead of writing the error page to minio-server
        # and timing out 60 s later in "Wait for MinIO".
        curl -fsSL -o minio-server "https://github.com/minio/minio/releases/download/${MINIO_RELEASE}/minio.darwin-arm64.${MINIO_RELEASE}"
        chmod +x minio-server
        mkdir -p "$RUNNER_TEMP/minio-data"
        MINIO_ROOT_USER=minioadmin MINIO_ROOT_PASSWORD=minioadmin \
          nohup ./minio-server server "$RUNNER_TEMP/minio-data" --address 127.0.0.1:9000 > minio.log 2>&1 &

    - name: Start MinIO (Windows)
      if: runner.os == 'Windows'
      shell: pwsh
      run: |
        Invoke-WebRequest -OutFile minio-server.exe -Uri "https://github.com/minio/minio/releases/download/${env:MINIO_RELEASE}/minio.windows-amd64.${env:MINIO_RELEASE}.exe"
        $env:MINIO_ROOT_USER = "minioadmin"
        $env:MINIO_ROOT_PASSWORD = "minioadmin"
        New-Item -ItemType Directory -Force -Path "$env:RUNNER_TEMP\minio-data" | Out-Null
        # Launch via pwsh in its own console.
        # Using Start-Process with -RedirectStandardOutput
        # makes the server inherit the console,
        # and terminates console-attached processes at console closure.
        Start-Process -WindowStyle Hidden -FilePath pwsh -ArgumentList @(
          '-NoProfile', '-Command',
          "./minio-server.exe server `"$env:RUNNER_TEMP\minio-data`" --address 127.0.0.1:9000 *> minio.log"
        )

    - name: Wait for MinIO
      shell: bash
      run: |
        for i in $(seq 1 60); do
          if curl -sf http://127.0.0.1:9000/minio/health/live; then
            echo "MinIO is up"
            exit 0
          fi
          sleep 1
        done
        echo "MinIO did not become healthy in time"
        echo "--- last connection attempt, verbose ---"
        curl -v --max-time 10 http://127.0.0.1:9000/minio/health/live || true
        echo "--- listening sockets on port 9000 ---"
        netstat -an | grep 9000 || true
        echo "--- minio processes still alive ---"
        tasklist 2>/dev/null | grep -i minio || ps aux | grep -i [m]inio || true
        echo "--- server log ---"
        cat minio.log minio-err.log 2>/dev/null || true
        exit 1

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v7
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install uv
      uses: astral-sh/setup-uv@v9.0.0
      with:
        cache-suffix: ${{ github.workflow }}

    - name: Sync Python environment
      run: uv sync

    - name: Test file-system only package
      run: |
        uv run --no-default-groups --group test-filesystem --isolated pytest --cov-fail-under=0 tests/test_minimal_install.py tests/test_backend_filesystem.py

    - name: Test with pytest
      run: |
        uv run pytest --cov-config=.coveragerc.${{ runner.os }}
